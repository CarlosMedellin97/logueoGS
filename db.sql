CREATE TABLE usuarios (usuarioID int unsigned NOT NULL AUTO_INCREMENT primary key,
NumeroEmpleado int NOT NULL, 
correo varchar(255) NOT NULL, 
password varchar(255) NOT NULL,
rol varchar(50) NOT NULL);
